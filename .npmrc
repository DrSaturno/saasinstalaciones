# Turbopack no resuelve los symlinks anidados de pnpm en este entorno
# (Windows + OneDrive): falla con "Module not found" en dependencias
# transitivas de Radix. node-linker=hoisted genera un node_modules plano,
# sin symlinks, igual que npm.
node-linker=hoisted
